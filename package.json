{
  "name": "synapse-bms",
  "private": true,
  "version": "0.1.0",
  "description": "Synapse Building Cloud monorepo",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start"
  }
}
